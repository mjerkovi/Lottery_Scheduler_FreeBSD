# $FreeBSD: tags/RELEASE_10_3_0/japanese/font-elisa10/files/Makefile.in 399948 2015-10-22 06:15:28Z hrs $

BDFTOPCF_CMD=	%%BDFTOPCF_CMD%%
GZIP_CMD=	%%GZIP_CMD%%

SRCS=	elisau10.bdf

.for F in ${SRCS:R}
${F}.pcf.gz: ${F}.bdf
	${BDFTOPCF_CMD} ${.ALLSRC} | ${GZIP_CMD} -c > ${.TARGET}

all: ${F}.pcf.gz
.endfor

.for F in ${SRCS:R}
${DESTDIR}%%DATADIR%%/${F}.pcf.gz: ${F}.pcf.gz
	mkdir -p ${DESTDIR}%%DATADIR%%
	${BSD_INSTALL_DATA} ${.ALLSRC} ${.TARGET}

install: ${DESTDIR}%%DATADIR%%/${F}.pcf.gz
.endfor

all install:
	@true
